package Day11.Ex02_Class;

import java.util.Arrays;
import java.util.Iterator;

public class ArraysEx {

	public static void main(String[] args) {

		int[] arr = {5,4,3,1,2}
		Arrays.sort(arr);
		
		
		for (int i = 0; i < arr.length; i++) {
			System.out.println(arr [i]);
			
			int[] random = new int[100];
		
		for (int i = 0; i < random.length; i++) {
			ranom[i] = (int)(Math.random() * 100 + 1 );
			
		}
		Arrays.sort(random);
		
		for (int i = 0; i < random.length; i++) {
			ranom[i] = (int)(Math.random() + " ");
			 
			System.out.println();
			
			
			// 이진탐색
		if (Arrays.binarySearch(random, 10) < 0 ) {
			System.out.println( "랜덤수 10이 없습니다");
		}	
		else {
			System.out.println("index : " + Arrays.binarySearch(random, 10));
		}
		int fill[] = new int[10]
		Arrays.fill(fill, 6);
		
		for (int i = 0; i < fill.length; i++) {
			System.out.println(fill[i] + " ");
		
		int copy[] = Arrays.copyOf(fill, 3)
				System.out.println
				
		for (int i = 0; i < fill.length; j++) {
			System.out.println(fill[i] + " ");
		
		int copyrange] = new int[10]
				Arrays.fill(fill, 6);
		
		for (int i = 0; i < fill.length; j++) {
			System.out.println(fill[i] + " ");
			
			
			
			
			
			
		}
			
		}
				
				
				
		
			
			
			
	}

}
